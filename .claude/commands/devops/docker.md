# /devops:docker

Docker 환경을 관리한다.

## Location
- `docker/docker-compose.yml` - 인프라 구성
- `docker/.env` - 환경 변수 (gitignore)
- `docker/.env.example` - 환경 변수 템플릿

## Instructions
1. `docker/` 디렉토리의 구성을 확인한다
2. 필요한 서비스를 추가/수정한다
3. `.env.example`을 함께 업데이트한다
